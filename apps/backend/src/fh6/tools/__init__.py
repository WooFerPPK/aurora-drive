"""Codegen / introspection CLIs used by the contract package and CI.

Tools are entry points only — they import from the rest of the
codebase but are not imported back. Each `python -m fh6.tools.<name>`
is its own executable.
"""
